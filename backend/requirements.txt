# ============================================================
# The Compliance Leader — Backend Dependencies
# Tested: Python 3.13 / Windows (no C++ Build Tools required)
#
# INSTALL WITH:
#   pip install --prefer-binary -r requirements.txt
#
# --prefer-binary forces pip to use pre-built wheels and never
# attempt to compile C/C++ extensions from source.
# ============================================================

# --- Core web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# --- Data validation ---
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator>=2.1.0       # required by pydantic EmailStr (used in auth routers)

# --- ISO 20022 / XML processing ---
lxml==5.3.0
xmlschema==3.4.2

# --- Logging ---
structlog==24.4.0

# --- Async I/O ---
aiofiles==24.1.0
httpx==0.28.0

# --- LLM providers ---
google-generativeai>=0.8.3
openai>=1.57.2

# --- LangChain stack (no strict pins — let pip resolve) ---
langchain>=0.3.12
langchain-google-genai>=2.0.7
langchain-openai>=0.2.12
langchain-community>=0.3.12

# --- Vector store ---
# >=0.6.0 ships pre-built Windows wheels for Python 3.13
chromadb>=0.6.0

# --- Embeddings ---
# sentence-transformers requires torch; torch 2.6+ supports Python 3.13
torch>=2.6.0
sentence-transformers>=3.3.1

# --- Document parsing ---
PyMuPDF==1.25.1
python-pptx==1.0.2
python-docx==1.1.2

# --- Python 3.13 compatibility pins ---
# Force pre-built wheel versions that ship Python 3.13 binaries:
PyYAML>=6.0.2          # older PyYAML fails to build on Python 3.13 (Cython issue)
grpcio>=1.64.0         # required by chromadb; >=1.64 has Python 3.13 wheels
numpy>=1.26.0          # numpy 1.26+ has Python 3.13 wheels

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.24.0

# --- Database ---
aiosqlite==0.20.0
sqlalchemy==2.0.36

# --- Auth (future use) ---
python-jose==3.3.0
passlib==1.7.4
bcrypt==4.2.1                # password hashing (used by auth_service)
PyJWT>=2.8.0                 # JWT token encoding/decoding
