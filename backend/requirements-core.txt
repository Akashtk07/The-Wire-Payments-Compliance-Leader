# ============================================================
# CORE requirements — installs cleanly on Python 3.13/Windows
# NO C++ compilation required. All packages have pre-built wheels.
#
# INSTALL:
#   pip install -r requirements-core.txt
# ============================================================

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Validation
pydantic==2.10.3
pydantic-settings==2.7.0

# XML / ISO 20022
lxml==5.3.0
xmlschema==3.4.2

# Logging
structlog==24.4.0

# Async I/O
aiofiles==24.1.0
httpx==0.28.0

# LLM API clients (pure Python wheels available)
google-generativeai>=0.8.3
grpcio>=1.64.0
openai>=1.57.2

# Document parsing (all have Python 3.13 Windows wheels)
PyMuPDF==1.25.1
python-pptx==1.0.2
python-docx==1.1.2

# YAML (must be >=6.0.2 for Python 3.13 wheels — older versions require Cython compilation)
PyYAML>=6.0.2

# Database
aiosqlite==0.20.0
sqlalchemy==2.0.36

# Auth (future)
python-jose==3.3.0
passlib==1.7.4

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
